apiVersion: v1
kind: ConfigMap
metadata:
  name: roundz-backend-config
  namespace: roundz
data:
  NODE_ENV: production
  CLOUD_PROVIDER: aws
  STORAGE_PROVIDER: s3
  PAYMENT_PROVIDER: razorpay
  PUSH_PROVIDER: fcm
